name: kuauth/credential-leak-queries-tests
version: 0.0.1
extractor: python
tests: .
dependencies:
  # Sibling local pack — resolved via codeql-workspace.yml at repo root.
  kuauth/credential-leak-queries: ${workspace}
  codeql/python-all: "*"
